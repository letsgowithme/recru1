{% extends 'base.html.twig' %}

{% block title %}Hello ApplyController!{% endblock %}

{% block body %}

<div class="container">
{# {% if is_granted('ROLE_CONSULTANT') %} #}
<ol>
{% for apply in applies %}
<li class="d-flex">
{{ apply.candidate.firstname }} - {{ apply.candidate.lastname }} - {{ apply.candidate.cvFilename }}
<br>
 - {{ apply.job.author }} 

<fieldset class="form-group">
      <div class="form-check form-switch">
     
        <input class="form-check-input ms-2" type="checkbox" id="flexSwitchCheckDefault">{{ apply.isApproved }}
         <label class="form-check-label ms-2" for="flexSwitchCheckDefault">Approuver</label>
        
      </div>
    </fieldset>
    {# <button class="btn btn-primary" type="submit" id="flexSwitch">Approuver</button> #}

   </li> 
{% endfor %}
</ol>
{# {% endif %} #}
</div>
   
{% endblock %}
